.agent/
.openvisio/

# Dashboard runtime data (regenerated by export_json.py / aggregate.py)
dashboard/data.json
dashboard/all-projects.json

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Thai README — local only, not pushed to GitHub
readme.th.md

# Internal notes — local only, not pushed to GitHub
readme.local.md

# Full internal spec — local only; public gets design.github.md instead
design.md

# Review artifacts — local only (not pushed)
opencode.json
scripts/cloudflare-audit-validate.cjs
scripts/e2e-openvisio.ps1
scripts/run_completion.py
fix-v2.md
sys_review.md

# Orchezer publish policy: orchestrator + scopeboard ship together as one repo.
# The lean apparatus below is published (test_logging imports verify_report,
# and test_lean_merge covers the 14 lean modules as a unit), so none of
# these files may stay ignored — a public clone must collect all tests.
# Still local-only: opencode.json / .mcp.json (secret risk — see *.example),
# design.md (internal; public spec is design.github.md), audit scripts,
# fix-v2.md / sys_review.md, baselines / plans / internal docs.
baseline-report.json
implementation-plan.md
docs/llm-telemetry-design.md
docs/ownership-policy.md
.mcp.json
